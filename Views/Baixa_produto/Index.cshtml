@model IEnumerable<New_controle.Models.EF.Baixa_produto>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Shampoo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Condicionador)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Sabonete)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Shampoo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Condicionador)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Sabonete)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Detalhes", "Details", new { id=item.Id }) |
            @Html.ActionLink("Apagar", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
